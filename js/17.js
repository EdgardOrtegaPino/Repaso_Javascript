// Condicionales

const disponible = 4000
const retirar = 200

if (disponible > retirar) {
    // se cu,ple la condicion y se ejecuta el codigo
    (console.log('Si puedes retirar'))
} else {
    // no se cumple la condcion, y se ejecuta este codigo
    console.log('Lo siento, no puedes retirar')
}


/* 
 * > - Mayor Que
 * > - Menor Que
 * >= Mayor o igual
 * <= Menor o igual
 */